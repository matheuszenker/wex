spring:
  datasource:
    url: jdbc:h2:mem:testdb
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: create-drop

treasury:
  api:
    base-url: http://api.fiscaldata.treasury.gov/services

logging:
  level:
    org.springframework: INFO
    com.wex: DEBUG
    org.hibernate.SQL: DEBUG
